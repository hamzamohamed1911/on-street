# Remote / shared default (.env)
NEXT_PUBLIC_API_URL=https://parkingapi.3utilities.com/api

# Local backend override (.env.local) — this file wins in `next dev`
# NEXT_PUBLIC_API_URL=http://localhost:8000/api
